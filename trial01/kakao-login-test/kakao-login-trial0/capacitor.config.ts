import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kakao-login-trial0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
