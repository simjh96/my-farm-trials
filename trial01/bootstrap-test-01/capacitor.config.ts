import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bootstrap-test-01',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
