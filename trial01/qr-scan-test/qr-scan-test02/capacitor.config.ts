import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'qr-scan-test02',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
