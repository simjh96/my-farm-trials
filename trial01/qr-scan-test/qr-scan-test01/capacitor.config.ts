import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'qr-scan-test01',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
